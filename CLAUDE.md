# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## リポジトリの現状

このリポジトリは**実装前**である。コードは存在せず、`documents/10_要件定義/` に日本語の要件定義書があるのみ。コミット履歴もまだない。

そのため、ビルド・Lint・テストのコマンドは未定義であり、技術スタックも未確定（後述）。実装を開始する際は、まず要件定義書を読み、技術選定を行う設計工程が前提になることを理解しておくこと。

## プロダクト概要（Voice Insight）

ブラウザ上でユーザーが一人で話した内容をAIで分析し、話し方の改善につなげる音声分析Webアプリ。**Phase1のスコープに限定して実装する**。

- **Phase1**: 話した「内容の構成」を **PREP（Point→Reason→Example→Point）** 観点で評価する。音声特徴（息継ぎ・速さ・トーン）の分析は行わない。
- **Phase2以降**: 音声特徴分析を追加（将来要件。今は実装しない）。

単一の流れで完結する: **録音画面 → 処理中画面（文字起こし・評価を自動実行）→ 評価結果画面**。各段階で失敗したら録音画面へ戻す導線を出す。

## 設計工程に委ねられている（＝勝手に決め打ちしない）事項

要件定義は「何を満たすか（What）」のみを規定し、以下は意図的に未確定。実装着手時はこれらを最初に決める必要がある（`07_システム・技術要件.md` 7.3）:

- フロント/バックエンドの技術スタック、ホスティング・デプロイ方式
- 文字起こし（STT）・構成評価に使うAIモデル／外部サービス
- 評価プロンプトと出力フォーマット、点数のスケール・算出基準（`04_評価要件.md` 4.4）
- 保存しない前提での音声データの一時的な取り回し（メモリ・通信の扱い）
- 応答時間などの性能目標値

## 実装時に必ず守る制約（Phase1）

これらは要件として確定済み。逸脱しないこと:

- **データを一切永続化しない**: 録音音声・文字起こし結果・評価結果のいずれも保存しない。評価結果はその場限りの一時情報（NF-03）。
- **ユーザー管理なし**: 登録・ログイン・履歴を作らない。
- **日本語のみ**対象（録音・文字起こし・評価、NF-02）。
- **録音は最大5分**（F-01）。
- **録音停止後は確認画面を挟まず**、追加操作なしで文字起こし→評価へ自動で進む。Phase1では文字起こし結果の確認・編集画面を作らない（F-02 / UC-01）。
- **PCブラウザが主対象**。スマホ対応はPhase1で必須としない（NF-01）。
- **プライバシー表示**: 「保存しない」前提と、外部AIへ音声・文字起こしが送信される可能性を画面上で明示する（NF-04 / 5.3）。
- **エラー時は再録音導線**を必ず出す（マイク不許可・録音／文字起こし／評価の失敗、NF-06）。
- 評価結果画面には最低限: 総合点 / PREP観点の評価 / 良い点 / 改善点 / 次回への具体的な改善提案（F-04）。

## 要件定義書の構成とトレーサビリティ

`documents/10_要件定義/` 配下。読み始めは `01_背景と目的.md` から。要件には識別子が付与されており、設計・実装・テストから参照する想定:

- `F-xx` 機能要件（`03_機能要件.md`）, `NF-xx` 非機能要件（`06_非機能要件.md`）
- `S-xx` 画面・状態（`05_画面要件.md`）, `UC-xx` ユースケース（`02_想定ユーザーとユースケース.md`）
- 評価の中身（観点・出力形式）は `04_評価要件.md`、受け入れ基準は `09_受け入れ基準.md`、用語は `10_用語集.md`

要件に関わる実装・変更を行うときは、対応する識別子（例: `F-03`, `NF-04`）を参照して整合を確認すること。
